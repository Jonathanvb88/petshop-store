# Furrow & Co. - Petshop Store Demo

Single-file static storefront with cart, checkout mock, and an Admin panel for adding products manually (form) or in bulk (CSV upload/download). Data persists in the browser's localStorage.

Deploy: import this repo into Vercel (Framework preset: Other / static).
